using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sBloque : MonoBehaviour
{
    /// <summary>
    /// Tamaño que va a tener el bloque
    /// </summary>
    public int iTamaño;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
